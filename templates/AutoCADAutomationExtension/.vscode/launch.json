{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to AutoCAD",
            "type": "clr",
            "request": "attach",
            "processName": "acad"
        },
        {
            "name": "Attach Process Id",
            "type": "clr",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}