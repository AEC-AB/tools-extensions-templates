{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Revit",
            "type": "clr",
            "request": "attach",
            "processName": "Revit"
        },
        {
            "name": "Attach Process Id",
            "type": "clr",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}